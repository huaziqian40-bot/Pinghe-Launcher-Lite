# 构建产物(可随时重建)
build/
dist/
*.msi
*.wixobj
*.wixpdb
# Python
__pycache__/
*.pyc
# 开发临时文件
_perf_*.py
_splash_*.py
_ui_*.py
_probe_*.py
*.log
msi_extract

# 开发临时文件(续) —— 这些脚本里硬编码了**真实账号与口令**, 绝不能入库
# (2026-09-21: `_crosscheck*.py` 曾被提交并推到公开仓库, 内含两位同学的真实邮箱与密码,
#  已用 `git rm --cached` 取消跟踪; 历史里的旧版本仍需按 README 的说明轮换口令/清理历史。)
_crosscheck*.py
_cleanup/
*.bak-*

# 隐私扫描用的"已知机密"清单：每行一个值，供 scripts/privacy_scan.py 精确匹配。
# **绝不能入库**（它装的就是机密本身）；不建也行，用 PHIX_SCAN_SECRETS 环境变量代替。
.secrets-scan.txt
*.bak-*.*
启动 Pinghe Launcher Lite.bat.bak*

# 产品安装包走 GitHub Releases, 不入库
PingheLauncherLite.exe
HelloPingheLauncher.exe
/deliver/
tools/wix314-binaries.zip
/tools/
*.dmg
*.icns

# 测试环境(便携数据 + 本地 exe, 不入库)
testenv/

# 本地工作文档与外部仓库克隆(不公开发布)
PH-Launcher/
INTEGRATION.md
CONTRIBUTOR-HANDOFF.md
NEW-AI-PROMPT.md
PHLauncher-DataIntegration-Handoff.zip
